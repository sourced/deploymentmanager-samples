# Example of the Cloud Router template usage.
#
# Replace the following placeholders with valid values:
#   <FIXME:networkName>: the URI of the network to which the router belongs
#

imports:
  - path: templates/cloud_router/cloud_router.py
    name: cloud_router.py

resources:
  - name: test-cloud-router
    type: cloud_router.py
    properties:
      name: cloud-router
      network: <FIXME:networkName>
      region: us-east1
      asn: 65001
